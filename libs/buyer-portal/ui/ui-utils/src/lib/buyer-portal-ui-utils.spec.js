import { buyerPortalUiUtils } from './buyer-portal-ui-utils';
describe('buyerPortalUiUtils', () => {
  it('should work', () => {
    expect(buyerPortalUiUtils()).toEqual('buyer-portal-ui-utils');
  });
});
