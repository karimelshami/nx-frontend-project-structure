import { buyerPortalUiComponents } from './buyer-portal-ui-components';
describe('buyerPortalUiComponents', () => {
  it('should work', () => {
    expect(buyerPortalUiComponents()).toEqual('buyer-portal-ui-components');
  });
});
