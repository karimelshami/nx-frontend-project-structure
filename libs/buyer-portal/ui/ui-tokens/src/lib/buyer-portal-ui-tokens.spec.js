import { buyerPortalUiTokens } from './buyer-portal-ui-tokens';
describe('buyerPortalUiTokens', () => {
  it('should work', () => {
    expect(buyerPortalUiTokens()).toEqual('buyer-portal-ui-tokens');
  });
});
