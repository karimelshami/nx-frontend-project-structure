import { buyerPortalUtilCore } from './buyer-portal-util-core';
describe('buyerPortalUtilCore', () => {
  it('should work', () => {
    expect(buyerPortalUtilCore()).toEqual('buyer-portal-util-core');
  });
});
