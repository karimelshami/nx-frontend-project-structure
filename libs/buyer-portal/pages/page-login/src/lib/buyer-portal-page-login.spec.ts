import { buyerPortalPageLogin } from './buyer-portal-page-login';

describe('buyerPortalPageLogin', () => {
  it('should work', () => {
    expect(buyerPortalPageLogin()).toEqual('buyer-portal-page-login');
  });
});
