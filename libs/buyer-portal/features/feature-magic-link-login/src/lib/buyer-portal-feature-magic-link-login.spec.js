import { buyerPortalFeatureMagicLinkLogin } from './buyer-portal-feature-magic-link-login';
describe('buyerPortalFeatureMagicLinkLogin', () => {
  it('should work', () => {
    expect(buyerPortalFeatureMagicLinkLogin()).toEqual(
      'buyer-portal-feature-magic-link-login'
    );
  });
});
